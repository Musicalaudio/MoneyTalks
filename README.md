Full-Stack application using the FinancialModellingPrep API, TypeScript, React, C#, and ASP.Net Core.

To run the frontend:
cd ./frontend && npm install && npm run start

To run the server:
cd ./api && dotnet watch run
